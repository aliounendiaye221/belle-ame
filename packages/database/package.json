{
  "name": "@belle-ame/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "npx --yes prisma generate",
    "db:generate": "npx --yes prisma generate",
    "db:push": "npx --yes prisma db push",
    "db:migrate": "npx --yes prisma migrate dev",
    "db:deploy": "npx --yes prisma migrate deploy",
    "db:seed": "ts-node --transpile-only prisma/seed.ts",
    "db:studio": "prisma studio",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "prisma": "^5.18.0"
  },
  "devDependencies": {
    "@belle-ame/config": "workspace:*",
    "@types/node": "^20.14.14",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
